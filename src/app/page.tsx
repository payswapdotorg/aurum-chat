// W000: the minimal Next.js shell. The Control Tower UI is a later work
// item — this page intentionally links to nothing.

export default function HomePage() {
  return (
    <main>
      <h1>Aurum</h1>
    </main>
  );
}
